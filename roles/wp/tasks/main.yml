---
# tasks file for common
- name: create directory
  file:
    path: /var/www/site
    state: directory

- name: download wp
  unarchive:
    src: https://wordpress.org/latest.tar.gz
    dest: "/var/www/site"
    remote_src: yes
    creates: "/var/www/site/wordpress"

- name: set ownership
  file:
    path: "/var/www/site"
    state: directory
    recurse: yes
    owner: www-data
    group: www-data

- name: set permission for directories
  shell: "/usr/bin/find /var/www/site/wordpress/ -type d -exec chmod 750 {} \\;"

- name: set permission for files
  shell: "/usr/bin/find /var/www/site/wordpress/ -type f -exec chmod 640 {} \\;"

#- name: setup wp-config
#  template:
#    src: "wp-config.php.j2"
#    dest: "/var/www/site/wordpress/wp-config.php"